/**
 * TEMPORARIO: `true` = sem tela de login, entra direto no app (e o root nao espera o bootstrap).
 * Quando quiser login de novo, mude para `false` e reinicie o Metro com --clear.
 */
export const bypassAuth = true;
